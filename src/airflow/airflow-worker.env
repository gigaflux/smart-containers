# The maximum number of concurrent task instances that this specific Celery worker
# can execute simultaneously. Scale this based on the CPU and RAM limits of the container.
# (Only applicable if using CeleryExecutor).
AIRFLOW__CELERY__WORKER_CONCURRENCY=8

